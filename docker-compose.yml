version: "3.5"

services:
  app:
    build: .
#    command: "/root/gtest"
    ports:
      - '8080'
    environment:
      REDIS_URL: redis:6379
    depends_on:
      - redis

  redis:
    image: redis:alpine
    ports:
      - '6379'
    logging:
      driver: none
